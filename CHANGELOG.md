# Changelog

## 0.1.0

- Migrated single-file script into a packaged `src/` Python project
- Added robust CLI with env + argument overrides
- Added retry-aware HTTP client and parser fallback handling
- Added Docker (`Dockerfile`, `docker-compose.yml`, entrypoint loop mode)
- Added `.env` and `.env.example`
- Added Ubuntu helper installer script
- Added unit tests for config and parser utility behavior
- Rewrote README with deployment and usage documentation
